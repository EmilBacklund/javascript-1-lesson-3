// Create a pet Object

const dogs = {
  breeds: ["Golden retriever", "Labrador", "Poodle"],
};

console.log(dogs.breeds);

// Network tab in inspect and then Fetch/XHR there you can see al the API's
